# ================================================
# 환경 변수 템플릿 (.env.example)
# ================================================
# 이 파일을 복사하여 .env 파일을 생성하고, 실제 값으로 변경하세요
# .env 파일은 .gitignore에 추가되어 Git에 커밋되지 않습니다
#
# 사용법:
#   1. 이 파일을 .env로 복사: cp .env.example .env
#   2. .env 파일을 열어서 실제 값으로 변경
#   3. 애플리케이션 실행 시 환경 변수로 자동 로드됨
# ================================================

# ========== JWT 설정 ==========
# JWT 비밀 키 (최소 64자 이상, HS512 알고리즘용)
# 프로덕션 환경에서는 강력한 무작위 문자열을 사용하세요
JWT_SECRET=mySecretKeyForJWTTokenGenerationThisKeyMustBeLongEnoughForHS512Algorithm

# Access Token 만료 시간 (밀리초)
# 기본값: 3600000 = 1시간
JWT_ACCESS_TOKEN_EXPIRATION=3600000

# Refresh Token 만료 시간 (밀리초)
# 기본값: 604800000 = 7일
JWT_REFRESH_TOKEN_EXPIRATION=604800000


# ========== 카카오 OAuth 설정 ==========
# 카카오 REST API 키 (카카오 개발자 콘솔에서 발급)
# https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=your_kakao_client_id_here

# 카카오 Client Secret (카카오 개발자 콘솔에서 발급)
KAKAO_CLIENT_SECRET=your_kakao_client_secret_here

# 카카오 로그인 후 리다이렉트 URI
# 개발: http://localhost:9080/auth/kakao/callback
# 프로덕션: https://yourdomain.com/auth/kakao/callback
KAKAO_REDIRECT_URI=http://localhost:9080/auth/kakao/callback


# ========== 데이터베이스 설정 ==========
# 데이터베이스 URL
# 개발: jdbc:mysql://localhost:3306/mannal?useSSL=false&serverTimezone=Asia/Seoul&connectionTimeZone=Asia/Seoul&forceConnectionTimeZoneToSession=true&allowPublicKeyRetrieval=true
# 프로덕션: jdbc:mysql://prod-db-server:3306/mannal?useSSL=true&serverTimezone=Asia/Seoul
DB_URL=jdbc:mysql://localhost:3306/mannal?useSSL=false&serverTimezone=Asia/Seoul&connectionTimeZone=Asia/Seoul&forceConnectionTimeZoneToSession=true&allowPublicKeyRetrieval=true

# 데이터베이스 사용자명
DB_USERNAME=root

# 데이터베이스 비밀번호
DB_PASSWORD=1234


# ================================================
# 주의사항
# ================================================
# 1. 실제 .env 파일은 절대 Git에 커밋하지 마세요!
# 2. 프로덕션 환경에서는 반드시 강력한 비밀번호를 사용하세요
# 3. JWT Secret은 최소 64자 이상의 무작위 문자열을 사용하세요
# 4. 카카오 Client ID/Secret은 카카오 개발자 콘솔에서 발급받으세요
# ================================================
